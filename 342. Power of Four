class Solution {
    public boolean isPowerOfFour(int n) {
       for(int i = 0;i<32;i++){
            double square = Math.pow(4,i);
            if (square == n){
                return true;
            }
            
        }
    return false;

    }
}
