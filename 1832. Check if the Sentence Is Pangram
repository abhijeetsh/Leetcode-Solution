class Solution {
    public boolean checkIfPangram(String sentence) {
        Set has = new HashSet<>();
        for(int i = 0;i<sentence.length();i++){
            has.add(sentence.charAt(i));
        }
        if(has.size() == 26){
            return true;
        }
        return false;
    }
}
